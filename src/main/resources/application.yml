spring:
  profiles:
    active: local
    include:
      - local-security
    group:
      local: local-db, oauth
      dev: dev-db, oauth
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  redis:
    host: localhost
    port: 6379
server:
  port: 8300
